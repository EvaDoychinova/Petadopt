import {Fragment} from 'react';

import './Contacts.css';

const Contacts=()=>{
    return(
        <Fragment>
            <h1>Contacts Page</h1>
        </Fragment>
    );
}

export default Contacts;